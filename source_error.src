# source_error.src

x = 1 + * 3        # el '*' está suelto: falta operando antes
if x > 0           # falta ':' al final de la condición
    y = x - 1      # sin ':' el parser no entrará en el if
else:
    y = x + 1
